import React from "react";
import Cart from "../../components/cart";

const CartScreen = ({ route, navigation }) => {
  return <Cart route={route} navigation={navigation} />;
};

export default CartScreen;
