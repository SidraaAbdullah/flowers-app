import React from "react";
import { SingleOrder } from "../components/order-history/components";

const SingleOrderHistoryScreen = ({ navigation, route }) => {
  return <SingleOrder navigation={navigation} route={route} />;
};
export { SingleOrderHistoryScreen };
